<section class="editorial-principles">
  <div class="app-layout">
    <div class="section-header">
      <span class="section-label">EDITORIAL PRINCIPLES</span>

      <h2>Built on trust, curiosity, and quality</h2>

      <p>
        Every story published on GrailDaily follows a simple philosophy: inspire
        curiosity while maintaining accuracy and clarity.
      </p>
    </div>

    <div class="principles-grid">
      <article class="principle-card">
        <h3>Accuracy</h3>

        <p>
          We prioritize credible sources and verified information whenever
          possible.
        </p>
      </article>

      <article class="principle-card">
        <h3>Curiosity</h3>

        <p>
          We encourage readers to explore unanswered questions and fascinating
          discoveries.
        </p>
      </article>

      <article class="principle-card">
        <h3>Accessibility</h3>

        <p>
          Complex historical and scientific topics are presented in a way that
          everyone can enjoy.
        </p>
      </article>
    </div>
  </div>
</section>
<style>
  .editorial-principles {
    padding: 90px 0;
  }

  .section-header {
    max-width: 760px;

    margin: 0 auto 72px;

    text-align: center;
  }

  .section-header h2 {
    margin: 18px 0;

    font-size: 2.7rem;
  }

  .section-header p {
    color: var(--muted-foreground);

    line-height: 1.8;
  }

  .principles-grid {
    display: grid;

    grid-template-columns: repeat(3, 1fr);

    gap: 28px;
  }

  .principle-card {
    padding: 42px;

    border-radius: 22px;

    border: 1px solid var(--border);

    background: var(--card);

    transition: 0.3s ease;
  }

  .principle-card:hover {
    transform: translateY(-6px);

    border-color: var(--accent);

    box-shadow: 0 16px 40px rgba(0, 0, 0, 0.08);
  }

  .principle-card h3 {
    margin-bottom: 18px;

    font-size: 1.5rem;
  }

  .principle-card p {
    color: var(--muted-foreground);

    line-height: 1.8;
  }

  @media (max-width: 768px) {
    .editorial-principles {
      padding: 56px 0;
    }

    .section-header {
      margin-bottom: 40px;
    }

    .section-header h2 {
      font-size: 2rem;
    }

    .principles-grid {
      grid-template-columns: 1fr;
      gap: 20px;
    }

    .principle-card {
      padding: 28px;
    }

    .principle-card h3 {
      font-size: 1.3rem;
    }

    .principle-card:hover {
      transform: none;
    }
  }
</style>
